import 'package:flutter/material.dart';

/// PurePlate AI color palette — warm coral + cream with food photography feel.
class AppColors {
  AppColors._();

  // Primary — Soft Coral
  static const Color coral = Color(0xFFE8735A);
  static const Color coralLight = Color(0xFFF4A293);
  static const Color coralDark = Color(0xFFD4543B);

  // Secondary — Warm Sage
  static const Color sage = Color(0xFF8BAF7C);
  static const Color sageLight = Color(0xFFB5D4A9);
  static const Color sageDark = Color(0xFF6A8F5C);

  // Backgrounds
  static const Color cream = Color(0xFFFFF8F0);
  static const Color warmWhite = Color(0xFFFFFDF9);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF2D2016);
  static const Color textSecondary = Color(0xFF6B5E54);
  static const Color textTertiary = Color(0xFF9B8E84);

  // Semantic
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFE53935);
  static const Color info = Color(0xFF42A5F5);

  // Expiry status
  static const Color fresh = Color(0xFF4CAF50);
  static const Color expiringSoon = Color(0xFFFF9800);
  static const Color expired = Color(0xFFE53935);

  // Surfaces
  static const Color divider = Color(0xFFE8E0D8);
  static const Color shimmer = Color(0xFFF5EDE5);
  static const Color bottomNavBackground = Color(0xFFFFFFFF);
  static const Color scrim = Color(0x66000000);
}
